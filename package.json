{
  "name": "@wallet-analytic/core",
  "type": "module",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter=@wallet-analytic/web dev & bun run --filter=@wallet-analytic/api dev && bun run --filter=@wallet-analytic/bot dev",
    "docker:rebuild": "docker compose -f docker-compose.dev.yml down -v && docker compose -f docker-compose.dev.yml up --build",
    "build:tsc": "tsc --build",

    "build": "bun run --filter=@wallet-analytic/web build && bun run --filter=@wallet-analytic/api build && bun run --filter=@wallet-analytic/bot build",

    "test": "bun test --recursive",
    "test:watch": "bun test --watch --recursive",

    "db:generate": "bun run --filter=@wallet-analytic/api db:generate",
    "db:migrate": "bun run --filter=@wallet-analytic/api db:migrate",

    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",

    "format": "biome format . --write",
    "checkAll": "biome check . --write --unsafe"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@biomejs/biome": "^1.9.4"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
